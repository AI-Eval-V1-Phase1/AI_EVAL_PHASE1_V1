import {
  pgTable,
  uuid,
  varchar,
  text,
  timestamp,
  jsonb,
} from "drizzle-orm/pg-core";

export const cotsBuyerAssessments = pgTable("cots_buyer_assessments", {
  id: uuid("id").defaultRandom().primaryKey(),
  assessment_id: uuid("assessment_id").notNull(),
  business_pain_point: text("business_pain_point"),
  expected_outcomes: varchar("expected_outcomes", { length: 300 }),
  owning_department: varchar("owning_department", { length: 100 }),
  budget_range: varchar("budget_range", { length: 100 }),
  target_timeline: varchar("target_timeline", { length: 100 }),
  criticality: varchar("criticality", { length: 100 }),
  vendor_name: varchar("vendor_name", { length: 200 }),
  product_name: varchar("product_name", { length: 200 }),
  requirement_gaps: text("requirement_gaps"),
  integration_systems: jsonb("integration_systems"),
  tech_stack: jsonb("tech_stack"),
  digital_maturity_level: varchar("digital_maturity_level", { length: 100 }),
  data_governance_maturity: varchar("data_governance_maturity", { length: 100 }),
  ai_governance_board: varchar("ai_governance_board", { length: 100 }),
  ai_ethics_policy: varchar("ai_ethics_policy", { length: 100 }),
  implementation_team_composition: jsonb("implementation_team_composition"),
  data_sensitivity: varchar("data_sensitivity", { length: 100 }),
  regulatory_requirements: jsonb("regulatory_requirements"),
  risk_appetite: varchar("risk_appetite", { length: 100 }),
  decision_stakes: varchar("decision_stakes", { length: 100 }),
  impacted_stakeholders: jsonb("impacted_stakeholders"),
  vendor_validation_approach: varchar("vendor_validation_approach", { length: 100 }),
  vendor_security_posture: varchar("vendor_security_posture", { length: 100 }),
  vendor_certifications: jsonb("vendor_certifications"),
  pilot_rollout_plan: varchar("pilot_rollout_plan", { length: 100 }),
  rollback_capability: varchar("rollback_capability", { length: 100 }),
  change_management_plan: varchar("change_management_plan", { length: 100 }),
  monitoring_data_available: varchar("monitoring_data_available", { length: 100 }),
  audit_logs_available: varchar("audit_logs_available", { length: 100 }),
  testing_results_available: varchar("testing_results_available", { length: 100 }),
  identified_risks: text("identified_risks"),
  risk_domain_scores: text("risk_domain_scores"),
  contextual_multipliers: text("contextual_multipliers"),
  risk_mitigation: text("risk_mitigation"),
  created_at: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
  updated_at: timestamp("updated_at", { withTimezone: true }).defaultNow().notNull(),
});
