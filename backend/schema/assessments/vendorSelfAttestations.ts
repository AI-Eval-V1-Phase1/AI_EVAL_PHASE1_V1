import {
  pgTable,
  uuid,
  varchar,
  text,
  timestamp,
  jsonb,
  integer,
} from "drizzle-orm/pg-core";

export const vendorSelfAttestations = pgTable("vendor_self_attestations", {
  id: uuid("id").defaultRandom().primaryKey(),
  /** Links to the logged-in vendor user; used for fetch/upsert when assessment_id is not required */
  user_id: integer("user_id"),
  assessment_id: uuid("assessment_id"),
  purchase_decision_makers: jsonb("purchase_decision_makers"),
  pain_points_solved: text("pain_points_solved"),
  alternatives_considered: text("alternatives_considered"),
  unique_value_proposition: text("unique_value_proposition"),
  typical_customer_roi: varchar("typical_customer_roi", { length: 500 }),
  ai_capabilities: jsonb("ai_capabilities"),
  ai_model_types: jsonb("ai_model_types"),
  model_transparency: varchar("model_transparency", { length: 100 }),
  decision_autonomy: varchar("decision_autonomy", { length: 100 }),
  security_certifications: jsonb("security_certifications"),
  assessment_completion_level: varchar("assessment_completion_level", { length: 100 }),
  pii_handling: varchar("pii_handling", { length: 100 }),
  data_residency_options: jsonb("data_residency_options"),
  data_retention_policy: text("data_retention_policy"),
  bias_testing_approach: jsonb("bias_testing_approach"),
  adversarial_security_testing: varchar("adversarial_security_testing", { length: 100 }),
  human_oversight: jsonb("human_oversight"),
  training_data_documentation: varchar("training_data_documentation", { length: 100 }),
  uptime_sla: varchar("uptime_sla", { length: 100 }),
  incident_response_plan: varchar("incident_response_plan", { length: 100 }),
  rollback_capability: varchar("rollback_capability", { length: 100 }),
  hosting_deployment: jsonb("hosting_deployment"),
  deployment_scale: varchar("deployment_scale", { length: 100 }),
  product_stage: varchar("product_stage", { length: 100 }),
  interaction_data_available: varchar("interaction_data_available", { length: 100 }),
  audit_logs_available: varchar("audit_logs_available", { length: 100 }),
  testing_results_available: varchar("testing_results_available", { length: 100 }),
  /** Stored document metadata: { "0": ["file1.pdf"], "1": [], "2": ["doc.docx"] } keyed by upload slot */
  document_uploads: jsonb("document_uploads"),
  created_at: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
  updated_at: timestamp("updated_at", { withTimezone: true }).defaultNow().notNull(),
});
