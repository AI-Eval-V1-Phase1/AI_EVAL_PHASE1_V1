import type { ReactNode } from "react"
const CardContainerOnBoarding = ({children}: { children: ReactNode }) => {
  return (
   <>
   <div>
    {children}
   </div>
   
   </>
  )
}

export default CardContainerOnBoarding