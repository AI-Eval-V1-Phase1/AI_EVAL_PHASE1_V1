version: "3.9"

services:
  frontend:
    build: ./frontend
    ports:
      - "3001:5173"
    depends_on:
      - backend

  backend:
    build: ./backend
    env_file:
      - ./backend/.env.local
    ports:
      - "5003:5003"
